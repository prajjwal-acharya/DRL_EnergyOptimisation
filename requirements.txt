# Direct dependencies pinned for the initial reproducible environment.
# Requires Python 3.9 exactly: on macOS use /usr/bin/python3 (system Python).
# Homebrew python3 is 3.14, which has no matplotlib 3.9.4 wheel and fails to
# build it from source.
CityLearn==2.5.0
pandas==2.3.3
numpy==1.26.4
matplotlib==3.9.4
jupyter==1.1.1
pytest==8.4.2
stable-baselines3==2.3.2
